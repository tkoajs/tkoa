language: node_js
node_js: 
    - '7.6'

cache:
  directories:
    - node_modules


install:
  - npm i --global ava@1.1.0
  - npm install --dependencies

before_script:

script:
  - npm run test

branches:
  only:
    - master
env:
 global:
